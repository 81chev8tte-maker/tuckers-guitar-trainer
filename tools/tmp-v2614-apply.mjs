import fs from 'node:fs';

function read(path){return fs.readFileSync(path,'utf8');}
function write(path,text){fs.writeFileSync(path,text);}
function replaceOnce(path,from,to,label=from.slice(0,60)){
  const text=read(path);
  if(!text.includes(from)) throw new Error(`${path}: missing anchor ${label}`);
  write(path,text.replace(from,to));
}
function replaceAllLiteral(path,from,to){
  const text=read(path);
  if(!text.includes(from)) throw new Error(`${path}: missing ${from}`);
  write(path,text.split(from).join(to));
}
function insertBefore(path,anchor,addition,label=anchor.slice(0,60)){
  replaceOnce(path,anchor,addition+anchor,label);
}

const versionFiles=['app.js','index.html','sw.js','package.json','package-lock.json','pwa-assets.test.js','guided-hardware-acceptance.js','guided-hardware-acceptance.test.js','browser-tests/app-smoke.spec.js'];
for(const path of versionFiles) replaceAllLiteral(path,'2.6.13','2.6.14');

const guided='guided-hardware-acceptance.js';

insertBefore(guided,
`  const HUMAN_QUESTIONS = [`,
`  const PIANO_MIC_QUIET_SAMPLE_COUNT = 12;\n  const PIANO_MIC_NOTES = [\n    { id:'c4', label:'C', note:'C4', midi:60 },\n    { id:'d4', label:'D', note:'D4', midi:62 },\n    { id:'e4', label:'E', note:'E4', midi:64 },\n    { id:'f4', label:'F', note:'F4', midi:65 },\n    { id:'g4', label:'G', note:'G4', midi:67 },\n    { id:'c4-repeat', label:'C again', note:'C4', midi:60, repeated:true }\n  ];\n`,
'Piano microphone constants');

insertBefore(guided,
`  function localDateStamp(value) {`,
`  const createPianoMicNoteResult = step => ({\n    expected:{ id:step.id, label:step.label, note:step.note, midi:step.midi, repeated:Boolean(step.repeated) },\n    detectedNote:null, detectedMidi:null, frequency:null, cents:null, confidence:0, level:0, peak:0, stable:false,\n    readings:0, attempts:0, retries:0, wrongDetections:[], passedAt:null\n  });\n  function applyPianoMicReading(result, step, reading) {\n    const next = clone(result);\n    next.readings += 1;\n    next.level = Number(reading?.level || 0);\n    next.peak = Math.max(Number(next.peak || 0), next.level);\n    next.confidence = Number(reading?.confidence || 0);\n    if (Number.isFinite(reading?.midi)) {\n      next.detectedMidi = reading.midi;\n      next.detectedNote = reading.name || null;\n      next.frequency = reading.frequency || null;\n      next.cents = Number.isFinite(reading.cents) ? reading.cents : null;\n    }\n    if (!reading?.stable) return { result:next, passed:false, reason:reading?.quiet ? 'quiet' : 'unstable' };\n    next.attempts += 1;\n    if (reading.midi !== step.midi) {\n      next.retries += 1;\n      next.wrongDetections.push({ note:reading.name || null, midi:Number.isFinite(reading.midi) ? reading.midi : null, frequency:reading.frequency || null });\n      next.wrongDetections = next.wrongDetections.slice(-6);\n      return { result:next, passed:false, reason:'wrong-note' };\n    }\n    next.stable = true;\n    next.passedAt = new Date().toISOString();\n    return { result:next, passed:true, reason:'passed' };\n  }\n  function createPianoMicrophoneState() {\n    return { status:'not-run', mode:'monophonic', quiet:null, notes:[], repeated:null, device:null, audioSettings:null, detectorRules:null, startedAt:null, completedAt:null };\n  }\n\n`,
'Piano microphone evidence helpers');

replaceOnce(guided,
`      format:'family-music-quest-guided-hardware-acceptance', version:2, appVersion:APP_VERSION, commit:null,`,
`      format:'family-music-quest-guided-hardware-acceptance', version:3, appVersion:APP_VERSION, commit:null,`,
'guided session schema version');
replaceOnce(guided,
`      guitar:{ status:'not-run', quiet:null, strings:[], repeated:null, silence:null },\n      midi:{ status:'not-run', device:null, noteOn:null, noteOff:null, velocitySamples:[], polyphonyMax:0, sustain:null, skipped:[] },`,
`      guitar:{ status:'not-run', quiet:null, strings:[], repeated:null, silence:null },\n      pianoMicrophone:createPianoMicrophoneState(),\n      midi:{ status:'not-run', device:null, noteOn:null, noteOff:null, velocitySamples:[], polyphonyMax:0, sustain:null, skipped:[] },`,
'guided session piano microphone field');
replaceOnce(guided,
`    if (session?.midi?.status !== 'complete') missing.push('Guided Piano MIDI capability test');\n    if (session?.midi?.sustain == null) missing.push('MIDI sustain capability (optional)');\n    missing.push('USB audio disconnect/reconnect (manual Monday test)');\n    missing.push('MIDI disconnect/reconnect (manual Monday test)');\n    missing.push('Piano microphone check (manual, one note at a time)');\n    missing.push('Physical end-to-end latency measurement');`,
`    if (session?.pianoMicrophone?.status !== 'complete') missing.push('Guided Piano microphone check (one note at a time)');\n    if (session?.midi?.status !== 'complete') missing.push('Guided Piano MIDI capability test');\n    if (session?.midi?.sustain == null) missing.push('MIDI sustain capability (optional)');\n    missing.push('USB audio disconnect/reconnect (manual Monday test)');\n    missing.push('MIDI disconnect/reconnect (manual Monday test)');\n    missing.push('Piano microphone gameplay / perceived response (manual physical test)');\n    missing.push('Physical end-to-end latency measurement');`,
'not-performed truth');
replaceOnce(guided,
`  const rules = { APP_VERSION, QUIET_SAMPLE_COUNT, GUITAR_STRINGS, HUMAN_QUESTIONS, scoreable, createNoteResult, applyNoteReading, summarizeQuiet, summarizeSilence, makeSessionId, normalizeEvidenceReferences, createHumanEvidence, createReportPayload, createSession, computeNotPerformed };`,
`  const rules = { APP_VERSION, QUIET_SAMPLE_COUNT, GUITAR_STRINGS, PIANO_MIC_QUIET_SAMPLE_COUNT, PIANO_MIC_NOTES, HUMAN_QUESTIONS, scoreable, createNoteResult, applyNoteReading, createPianoMicNoteResult, applyPianoMicReading, summarizeQuiet, summarizeSilence, makeSessionId, normalizeEvidenceReferences, createHumanEvidence, createReportPayload, createSession, computeNotPerformed };`,
'rules export');

replaceOnce(guided,
`      if (!session.humanEvidence) { session.humanEvidence = createHumanEvidence(); changed = true; }`,
`      if (!session.humanEvidence) { session.humanEvidence = createHumanEvidence(); changed = true; }\n      if (!session.pianoMicrophone) { session.pianoMicrophone = createPianoMicrophoneState(); changed = true; }\n      if (Number(session.version || 0) < 3) { session.version = 3; changed = true; }`,
'legacy guided session backfill');

replaceOnce(guided,
`  let guidedMidiUnsub = null;\n  let guidedMidiOwned = false;`,
`  let guidedMidiUnsub = null;\n  let guidedMidiOwned = false;\n  let guidedPianoMic = null;\n  let pianoMicNoteIndex = 0;\n  let pianoMicNoteResult = null;\n  let pianoMicQuietSamples = [];`,
'Piano mic runtime state');

replaceOnce(guided,
`    launch.innerHTML = '<button id="diagRunGuided" class="button big" type="button">▶ Run Hardware Test</button><span>Short kid-friendly checks. Technical details are recorded automatically.</span>';`,
`    launch.innerHTML = '<button id="diagRunGuided" class="button big" type="button">🧪 Quick Hardware Tests</button><span>Short kid-friendly checks. Family Music Quest records the technical details for you.</span>';`,
'Quick Hardware Tests launch');
replaceOnce(guided,
`    tab.textContent = '🧪 Guided Test';`,
`    tab.textContent = '🧪 Quick Tests';`,
'Quick Tests tab');
replaceOnce(guided,
`      <div class="guided-title-row"><div><p class="eyebrow">MONDAY HARDWARE ACCEPTANCE</p><h2>Guided Hardware Test</h2></div><span id="guidedProgress" class="badge">Ready</span></div>\n      <p id="guidedLead" class="muted">Pick a test. You only need to follow the big instructions — Family Music Quest records the technical details.</p>\n      <div id="guidedMenu" class="guided-menu">\n        <button id="guidedStartGuitar" class="guided-choice" type="button"><span>🎸</span><strong>Guitar audio</strong><small>Quiet check, all six open strings, repeated note and silence.</small></button>\n        <button id="guidedStartMidi" class="guided-choice" type="button"><span>🎹</span><strong>Piano MIDI</strong><small>Note On/Off, velocity, chord/polyphony and optional sustain.</small></button>\n      </div>`,
`      <div class="guided-title-row"><div><p class="eyebrow">SHORT CHILD-FRIENDLY CHECKS</p><h2>Quick Hardware Tests</h2></div><span id="guidedProgress" class="badge">Ready</span></div>\n      <p id="guidedLead" class="muted">Pick one test and follow the big instructions. Technical details stay in the report.</p>\n      <div id="guidedMenu" class="guided-menu">\n        <button id="guidedStartGuitar" class="guided-choice" type="button"><span>🎸</span><strong>Test Guitar Microphone</strong><small>Quiet check, all six open strings, repeated note and silence.</small></button>\n        <button id="guidedStartPianoMic" class="guided-choice" type="button"><span>🎹</span><strong>Test Piano Microphone</strong><small>One note at a time: C–D–E–F–G, then C again.</small></button>\n        <button id="guidedStartMidi" class="guided-choice" type="button"><span>🎹</span><strong>Test MIDI Keyboard</strong><small>Note On/Off, velocity, chord/polyphony and optional sustain.</small></button>\n      </div>`,
'Quick Tests menu');
replaceOnce(guided,
`    $('guidedStartGuitar').onclick = () => startGuitar(false).catch(showGuidedError);\n    $('guidedStartMidi').onclick = () => startMidi(false).catch(showGuidedError);`,
`    $('guidedStartGuitar').onclick = () => startGuitar(false).catch(showGuidedError);\n    $('guidedStartPianoMic').onclick = () => startPianoMic(false).catch(showGuidedError);\n    $('guidedStartMidi').onclick = () => startMidi(false).catch(showGuidedError);`,
'Quick Tests handlers');

replaceOnce(guided,
`    $('guidedProgress').textContent = !s ? 'Ready' : s.status === 'complete' ? 'Report saved' : path === 'guitar' ? 'Guitar' : path === 'midi' ? 'MIDI' : 'In progress';\n    $('guidedLead').textContent = s ? \`Guitar: \${statusText(s.guitar.status)} · MIDI: \${statusText(s.midi.status)}. Raw measurements stay in the report.\` : 'Pick a test. You only need to follow the big instructions — Family Music Quest records the technical details.';\n    if (!path) {\n      $('guidedStartGuitar').querySelector('small').textContent = \`\${statusText(s?.guitar?.status)} · Quiet check, six strings, repeated note and silence.\`;\n      $('guidedStartMidi').querySelector('small').textContent = \`\${statusText(s?.midi?.status)} · Note On/Off, velocity, polyphony and optional sustain.\`;\n    }\n    if (path === 'guitar') renderGuitarTask();\n    if (path === 'midi') renderMidiTask();`,
`    $('guidedProgress').textContent = !s ? 'Ready' : s.status === 'complete' ? 'Report saved' : path === 'guitar' ? 'Guitar' : path === 'piano-mic' ? 'Piano microphone' : path === 'midi' ? 'MIDI' : 'In progress';\n    $('guidedLead').textContent = s ? \`Guitar mic: \${statusText(s.guitar.status)} · Piano mic: \${statusText(s.pianoMicrophone?.status)} · MIDI: \${statusText(s.midi.status)}.\` : 'Pick one test and follow the big instructions. Technical details stay in the report.';\n    if (!path) {\n      $('guidedStartGuitar').querySelector('small').textContent = \`\${statusText(s?.guitar?.status)} · Quiet check, six strings, repeated note and silence.\`;\n      $('guidedStartPianoMic').querySelector('small').textContent = \`\${statusText(s?.pianoMicrophone?.status)} · One note at a time: C–D–E–F–G, then C again.\`;\n      $('guidedStartMidi').querySelector('small').textContent = \`\${statusText(s?.midi?.status)} · Note On/Off, velocity, polyphony and optional sustain.\`;\n    }\n    if (path === 'guitar') renderGuitarTask();\n    if (path === 'piano-mic') renderPianoMicTask();\n    if (path === 'midi') renderMidiTask();`,
'render quick test statuses');

insertBefore(guided,
`  function renderMidiTask() {`,
`  function renderPianoMicTask() {\n    $('guidedSkip').hidden = true;\n    $('guidedAction').hidden = true;\n    if (phase === 'piano-mic-quiet') {\n      setTask(\`PIANO MICROPHONE · QUIET \${Math.min(pianoMicQuietSamples.length, PIANO_MIC_QUIET_SAMPLE_COUNT)} / \${PIANO_MIC_QUIET_SAMPLE_COUNT}\`, 'Stay quiet for a moment', 'Microphone mode listens to one piano note at a time. No chords in this test.');\n      return;\n    }\n    if (phase === 'piano-mic-note') {\n      const step = PIANO_MIC_NOTES[pianoMicNoteIndex];\n      const prompt = step.repeated ? 'Play C again' : \`Play \${step.label}\`;\n      setTask(\`PIANO MICROPHONE · \${pianoMicNoteIndex + 1} OF \${PIANO_MIC_NOTES.length}\`, prompt, message || \`Waiting for \${step.note}… One note at a time.\`, pianoMicNoteResult?.level || 0);\n    }\n  }\n\n`,
'Piano mic task renderer');

insertBefore(guided,
`  async function startMidi(isSynthetic=false) {`,
`  async function startPianoMic(isSynthetic=false) {\n    ensureSession(); cleanupResources();\n    synthetic = isSynthetic; path = 'piano-mic'; phase = 'piano-mic-quiet'; message = ''; pianoMicNoteIndex = 0; pianoMicNoteResult = null; pianoMicQuietSamples = [];\n    session.pianoMicrophone = { ...createPianoMicrophoneState(), status:'running', startedAt:new Date().toISOString() };\n    persistSession(); renderGuided();\n    if (isSynthetic) return;\n    const PianoMic = window.NovaPianoInputs?.MicrophonePianoInput;\n    if (!PianoMic) throw new Error('Piano microphone input is still loading.');\n    guidedPianoMic = new PianoMic({ emit:()=>{} });\n    await guidedPianoMic.start(processPianoMicReading);\n    const track = guidedPianoMic.stream?.getAudioTracks?.()[0] || guidedPianoMic.stream?.getTracks?.()[0];\n    const settings = track?.getSettings?.() || {};\n    session.pianoMicrophone.device = { id:settings.deviceId || '', label:track?.label || 'Default microphone' };\n    session.pianoMicrophone.audioSettings = { sampleRate:guidedPianoMic.context?.sampleRate || settings.sampleRate || null, channelCount:settings.channelCount || 1 };\n    session.pianoMicrophone.detectorRules = clone(window.NovaPianoInputs.MIC_RULES || {});\n    persistSession();\n  }\n  function processPianoMicReading(reading) {\n    if (path !== 'piano-mic' || reading?.active === false) return;\n    if ($('guidedSignal')) $('guidedSignal').style.width = \`\${Math.min(100, Number(reading?.level || 0) * 1600)}%\`;\n    if (phase === 'piano-mic-quiet') {\n      pianoMicQuietSamples.push(Number(reading?.level || 0));\n      if (pianoMicQuietSamples.length >= PIANO_MIC_QUIET_SAMPLE_COUNT) {\n        session.pianoMicrophone.quiet = { ...summarizeQuiet(pianoMicQuietSamples), measuredAt:new Date().toISOString(), measurementOnly:true };\n        phase = 'piano-mic-note'; pianoMicNoteIndex = 0; pianoMicNoteResult = createPianoMicNoteResult(PIANO_MIC_NOTES[0]); message = '';\n        persistSession();\n      }\n      renderGuided(); return;\n    }\n    if (phase !== 'piano-mic-note') return;\n    const step = PIANO_MIC_NOTES[pianoMicNoteIndex];\n    const applied = applyPianoMicReading(pianoMicNoteResult, step, reading);\n    pianoMicNoteResult = applied.result;\n    if (applied.reason === 'quiet') message = 'Too quiet — play a little louder.';\n    else if (applied.reason === 'unstable') message = 'We can hear something, but the note is not steady yet. Try one clean note.';\n    else if (applied.reason === 'wrong-note') message = \`We heard \${reading.name || 'another note'}. Try \${step.label} again.\`;\n    if (applied.passed) {\n      message = \`Great — we heard \${step.label}.\`;\n      if (step.repeated) session.pianoMicrophone.repeated = pianoMicNoteResult;\n      else session.pianoMicrophone.notes.push(pianoMicNoteResult);\n      pianoMicNoteIndex += 1;\n      if (pianoMicNoteIndex >= PIANO_MIC_NOTES.length) { finishPianoMicPath(); return; }\n      pianoMicNoteResult = createPianoMicNoteResult(PIANO_MIC_NOTES[pianoMicNoteIndex]);\n    }\n    persistSession(); renderGuided();\n  }\n  function finishPianoMicPath() {\n    session.pianoMicrophone.status = 'complete';\n    session.pianoMicrophone.completedAt = new Date().toISOString();\n    cleanupPianoMic(); path = null; phase = null; message = '';\n    persistSession();\n    if (!synthetic) { showQuestions(); return; }\n    renderGuided();\n  }\n\n`,
'Piano mic guided runtime');

replaceOnce(guided,
`  function finishGuitarPath() {\n    const summary = summarizeSilence(silenceReadings, noiseGate);\n    session.guitar.silence = { ...summary, measuredAt:new Date().toISOString() };\n    session.guitar.status = 'complete';\n    if (summary.scoreableReadings > 0) session.warnings.push(\`Guitar input produced \${summary.scoreableReadings} scoreable reading(s) during the guided silence check.\`);\n    session.warnings = [...new Set(session.warnings)];\n    cleanupAudio(); path = null; phase = null; message = '';\n    persistSession(); renderGuided();\n  }`,
`  function finishGuitarPath() {\n    const summary = summarizeSilence(silenceReadings, noiseGate);\n    session.guitar.silence = { ...summary, measuredAt:new Date().toISOString() };\n    session.guitar.status = 'complete';\n    if (summary.scoreableReadings > 0) session.warnings.push(\`Guitar input produced \${summary.scoreableReadings} scoreable reading(s) during the guided silence check.\`);\n    session.warnings = [...new Set(session.warnings)];\n    cleanupAudio(); path = null; phase = null; message = '';\n    persistSession();\n    if (!synthetic) { showQuestions(); return; }\n    renderGuided();\n  }`,
'Guitar real quick-test completion');
replaceOnce(guided,
`  function finishMidiPath() {\n    session.midi.status = session.midi.skipped.length ? 'partial' : 'complete';\n    cleanupMidi(); path = null; phase = null; message = '';\n    persistSession(); renderGuided();\n  }`,
`  function finishMidiPath() {\n    session.midi.status = session.midi.skipped.length ? 'partial' : 'complete';\n    cleanupMidi(); path = null; phase = null; message = '';\n    persistSession();\n    if (!synthetic) { showQuestions(); return; }\n    renderGuided();\n  }`,
'MIDI real quick-test completion');

replaceOnce(guided,
`  function cleanupResources() { cleanupAudio(); cleanupMidi(); }`,
`  function cleanupPianoMic() {\n    if (guidedPianoMic) { try { guidedPianoMic.stop(); } catch {} }\n    guidedPianoMic = null;\n    pianoMicQuietSamples = []; pianoMicNoteResult = null; pianoMicNoteIndex = 0;\n  }\n  function cleanupResources() { cleanupAudio(); cleanupPianoMic(); cleanupMidi(); }`,
'Piano mic cleanup');

replaceOnce(guided,
`    form.innerHTML = \`<h3>Five quick human checks</h3><p class="muted">These are your observations, not automatic measurements. Good = no problem; Bad = a clear problem.</p>`,
`    form.innerHTML = \`<h3>✅ Done! Five quick questions</h3><p class="muted">Answer what you actually noticed. Good = no problem; Bad = a clear problem.</p>`,
'child completion questions heading');

insertBefore(guided,
`  function midiProjectSummary(s) {`,
`  function pianoMicProjectSummary(s) {\n    const mic = s?.pianoMicrophone || {};\n    if (mic.status === 'not-run' || !mic.status) return 'NOT RUN';\n    const notes = mic.notes || [];\n    const retries = [...notes, ...(mic.repeated ? [mic.repeated] : [])].reduce((sum,item)=>sum+Number(item.retries || 0),0);\n    return \`\${String(mic.status || 'unknown').toUpperCase()} · monophonic · \${notes.length}/5 C–G notes passed · repeated C \${mic.repeated?.stable ? 'passed' : 'not passed'} · retries \${retries}\`;\n  }\n`,
'Piano mic project report summary');
replaceOnce(guided,
`    lines.push('', 'AUTOMATED EVIDENCE', \`Guitar: \${guitarProjectSummary(s)}\`, \`Piano/MIDI: \${midiProjectSummary(s)}\`);`,
`    lines.push('', 'AUTOMATED EVIDENCE', \`Guitar microphone: \${guitarProjectSummary(s)}\`, \`Piano microphone: \${pianoMicProjectSummary(s)}\`, \`Piano/MIDI: \${midiProjectSummary(s)}\`);`,
'Project report Piano microphone line');

replaceOnce(guided,
`    $('guidedSummary').innerHTML = \`<strong>✅ Hardware test report saved</strong><p>Session: \${session.sessionId || '—'} · Adult result: \${formatAdultResult(session.humanEvidence?.adultResult)}</p><p>Guitar: \${statusText(session.guitar.status)} · MIDI: \${statusText(session.midi.status)}</p><p class="muted">Open Report to copy, share or download the evidence. Monday physical gameplay checks are still required.</p><div class="diagnostic-actions"><button id="guidedViewReport" class="button" type="button">View Report</button><button id="guidedNewSession" class="button secondary" type="button">Start New Test</button></div>\`;\n    $('guidedViewReport').onclick = () => {`,
`    $('guidedSummary').innerHTML = \`<strong>✅ Done! Hardware test report saved</strong><p>Session: \${session.sessionId || '—'} · Adult result: \${formatAdultResult(session.humanEvidence?.adultResult)}</p><p>Guitar mic: \${statusText(session.guitar.status)} · Piano mic: \${statusText(session.pianoMicrophone?.status)} · MIDI: \${statusText(session.midi.status)}</p><p class="muted">This records what you tested. It does not automatically approve the release.</p><div class="diagnostic-actions"><button id="guidedSendParent" class="button" type="button">Send Report to Parent</button><button id="guidedViewReport" class="button secondary" type="button">View Report</button><button id="guidedNewSession" class="button secondary" type="button">Start New Test</button></div><p id="guidedShareStatus" class="muted" aria-live="polite"></p>\`;\n    $('guidedSendParent').onclick = async () => {\n      const result = await shareTestReport();\n      const status = $('guidedShareStatus');\n      if (!status) return;\n      status.textContent = result.status === 'shared' ? 'Share opened. Choose the parent destination and confirm Send.' : result.status === 'cancelled' ? 'Sharing cancelled. Your report is still saved.' : result.status === 'downloaded' ? 'Native sharing is unavailable, so the JSON fallback was downloaded.' : 'Could not open sharing. Your report is still saved; use View Report for Copy/Download fallbacks.';\n    };\n    $('guidedViewReport').onclick = () => {`,
'Quick-test summary Send Report action');

replaceOnce(guided,
`    getState:() => ({ path, phase, stringIndex, resources:{ audioSubscribed:Boolean(guidedAudioUnsub), audioOwned:guidedAudioOwned, midiSubscribed:Boolean(guidedMidiUnsub), midiOwned:guidedMidiOwned } }),`,
`    getState:() => ({ path, phase, stringIndex, pianoMicNoteIndex, resources:{ audioSubscribed:Boolean(guidedAudioUnsub), audioOwned:guidedAudioOwned, pianoMicActive:Boolean(guidedPianoMic), midiSubscribed:Boolean(guidedMidiUnsub), midiOwned:guidedMidiOwned } }),`,
'Quick-test API state');
replaceOnce(guided,
`    beginGuitarSynthetic:() => startGuitar(true),\n    feedAudio:reading => processAudioReading(reading),\n    startSilenceSynthetic:startSilenceCheck,\n    beginMidiSynthetic:() => startMidi(true),`,
`    beginGuitarSynthetic:() => startGuitar(true),\n    feedAudio:reading => processAudioReading(reading),\n    startSilenceSynthetic:startSilenceCheck,\n    beginPianoMicSynthetic:() => startPianoMic(true),\n    feedPianoMic:reading => processPianoMicReading(reading),\n    beginMidiSynthetic:() => startMidi(true),`,
'Quick-test synthetic Piano API');

// Node rule coverage for Piano microphone truth and gating.
const nodeTest='guided-hardware-acceptance.test.js';
insertBefore(nodeTest,
`const payloadReport = {`,
`assert.equal(rules.PIANO_MIC_NOTES.length, 6);\nassert.deepEqual(rules.PIANO_MIC_NOTES.map(step=>step.midi), [60,62,64,65,67,60]);\nconst c4 = rules.PIANO_MIC_NOTES[0];\nlet pianoResult = rules.createPianoMicNoteResult(c4);\nlet pianoApplied = rules.applyPianoMicReading(pianoResult, c4, {active:true,level:.001,quiet:true,stable:false});\nassert.equal(pianoApplied.passed, false, 'quiet Piano candidate cannot pass');\npianoApplied = rules.applyPianoMicReading(pianoApplied.result, c4, {active:true,level:.03,midi:60,name:'C4',stable:false,confidence:.9});\nassert.equal(pianoApplied.passed, false, 'unstable Piano candidate cannot pass');\npianoApplied = rules.applyPianoMicReading(pianoApplied.result, c4, {active:true,level:.03,midi:62,name:'D4',stable:true,confidence:.9});\nassert.equal(pianoApplied.passed, false, 'wrong stable Piano note cannot pass');\nassert.equal(pianoApplied.result.retries, 1);\npianoApplied = rules.applyPianoMicReading(pianoApplied.result, c4, {active:true,level:.03,midi:60,name:'C4',stable:true,confidence:.9,frequency:261.63,cents:0});\nassert.equal(pianoApplied.passed, true, 'correct production-gated stable Piano reading passes');\nassert.equal(pianoApplied.result.stable, true);\n\nconst pianoMissingSession = rules.createSession();\nassert(rules.computeNotPerformed(pianoMissingSession).some(x=>x.includes('Guided Piano microphone')));\npianoMissingSession.pianoMicrophone.status = 'complete';\nconst afterPianoGuide = rules.computeNotPerformed(pianoMissingSession);\nassert(!afterPianoGuide.some(x=>x.includes('Guided Piano microphone')), 'completed guided Piano microphone must not be reported unperformed');\nassert(afterPianoGuide.some(x=>x.includes('Piano microphone gameplay / perceived response')), 'guided Piano microphone must not claim gameplay/latency acceptance');\n\n`,
'Node Piano mic tests');
replaceOnce(nodeTest,`assert.equal(session.version, 2);`,`assert.equal(session.version, 3);`,'guided session version test');

// Browser: current report schema version and focused synthetic Quick Test coverage.
const smoke='browser-tests/app-smoke.spec.js';
replaceOnce(smoke,`expect(report.guidedAcceptance.version).toBe(2);`,`expect(report.guidedAcceptance.version).toBe(3);`,'browser guided schema version');

const quickSpec=`const {test,expect}=require('@playwright/test');\n\nasync function openQuickTests(page){\n  await page.goto('/');\n  await page.getByLabel('Your name').fill('Quick Hardware Child');\n  await page.getByRole('button',{name:'Continue'}).click();\n  await page.getByRole('button',{name:'Start Playing'}).click();\n  await page.getByRole('button',{name:/Hardware & Backup/}).click();\n  await page.getByRole('button',{name:/Quick Hardware Tests/}).click();\n  await expect(page.getByRole('heading',{name:'Quick Hardware Tests'})).toBeVisible();\n}\n\ntest('Quick Piano microphone guide rejects quiet, unstable and wrong notes and records truthful evidence',async({page})=>{\n  await openQuickTests(page);\n  await expect(page.getByRole('button',{name:/Test Guitar Microphone/})).toBeVisible();\n  await expect(page.getByRole('button',{name:/Test Piano Microphone/})).toBeVisible();\n  await expect(page.getByRole('button',{name:/Test MIDI Keyboard/})).toBeVisible();\n\n  await page.evaluate(()=>window.FMQGuidedHardwareTest.beginPianoMicSynthetic());\n  await page.evaluate(()=>{for(let i=0;i<12;i++)window.FMQGuidedHardwareTest.feedPianoMic({active:true,level:.001,quiet:true,stable:false});});\n  await expect(page.locator('#guidedPrompt')).toHaveText('Play C');\n  expect((await page.evaluate(()=>window.FMQGuidedHardwareTest.getState())).pianoMicNoteIndex).toBe(0);\n\n  await page.evaluate(()=>window.FMQGuidedHardwareTest.feedPianoMic({active:true,level:.03,midi:60,name:'C4',stable:false,confidence:.9}));\n  expect((await page.evaluate(()=>window.FMQGuidedHardwareTest.getState())).pianoMicNoteIndex).toBe(0);\n  await expect(page.locator('#guidedMessage')).toContainText('not steady');\n\n  await page.evaluate(()=>window.FMQGuidedHardwareTest.feedPianoMic({active:true,level:.03,midi:62,name:'D4',stable:true,confidence:.9}));\n  expect((await page.evaluate(()=>window.FMQGuidedHardwareTest.getState())).pianoMicNoteIndex).toBe(0);\n  await expect(page.locator('#guidedMessage')).toContainText('We heard D4');\n\n  for(const [midi,name] of [[60,'C4'],[62,'D4'],[64,'E4'],[65,'F4'],[67,'G4'],[60,'C4']]){\n    await page.evaluate(({midi,name})=>window.FMQGuidedHardwareTest.feedPianoMic({active:true,level:.03,midi,name,stable:true,confidence:.9,frequency:440*Math.pow(2,(midi-69)/12),cents:0}),{midi,name});\n  }\n  const session=await page.evaluate(()=>window.FMQGuidedHardwareTest.getSession());\n  expect(session.pianoMicrophone.status).toBe('complete');\n  expect(session.pianoMicrophone.mode).toBe('monophonic');\n  expect(session.pianoMicrophone.notes.map(item=>item.expected.midi)).toEqual([60,62,64,65,67]);\n  expect(session.pianoMicrophone.repeated.expected.midi).toBe(60);\n  expect(session.pianoMicrophone.repeated.stable).toBe(true);\n\n  await page.locator('#guidedFinish').click();\n  await expect(page.locator('#guidedQuestions')).toBeVisible();\n  await expect(page.locator('#guidedQuestions')).toContainText('Done! Five quick questions');\n  for(const id of ['reaction','delay','smooth','readability','keepPlaying']) await page.locator('input[name="guided-'+id+'"][value="good"]').check();\n  await page.locator('input[name="guided-scoring-trust"][value="mostly"]').check();\n  await expect(page.locator('#guidedAdultResult')).toHaveValue('not-decided');\n  await page.getByRole('button',{name:'Save Answers & Finish'}).click();\n  await expect(page.locator('#guidedSummary')).toContainText('Done! Hardware test report saved');\n  await expect(page.getByRole('button',{name:'Send Report to Parent'})).toBeVisible();\n\n  const report=await page.evaluate(()=>window.FMQGuidedHardwareTest.reportObject());\n  expect(report.appVersion).toBe('2.6.14');\n  expect(report.guidedAcceptance.version).toBe(3);\n  expect(report.guidedAcceptance.humanEvidence.adultResult).toBe('not-decided');\n  expect(report.guidedAcceptance.testsNotPerformed.some(item=>item.includes('Guided Piano microphone'))).toBe(false);\n  expect(report.guidedAcceptance.testsNotPerformed.some(item=>item.includes('Piano microphone gameplay / perceived response'))).toBe(true);\n  expect(await page.evaluate(()=>window.FMQGuidedHardwareTest.reportText())).toContain('Piano microphone: COMPLETE · monophonic');\n});\n\ntest('unperformed Quick Piano microphone remains explicit while Guitar and MIDI synthetic paths remain callable',async({page})=>{\n  await openQuickTests(page);\n  const initial=await page.evaluate(()=>{\n    window.FMQGuidedHardwareTest.beginNew();\n    return window.FMQGuidedHardwareTest.getSession();\n  });\n  expect(initial.pianoMicrophone.status).toBe('not-run');\n  expect((await page.evaluate(()=>window.FMQGuidedHardwareTest.rules.computeNotPerformed(window.FMQGuidedHardwareTest.getSession()))).some(item=>item.includes('Guided Piano microphone'))).toBe(true);\n  await page.evaluate(()=>window.FMQGuidedHardwareTest.beginGuitarSynthetic());\n  expect((await page.evaluate(()=>window.FMQGuidedHardwareTest.getState())).path).toBe('guitar');\n  await page.evaluate(()=>window.FMQGuidedHardwareTest.beginMidiSynthetic());\n  expect((await page.evaluate(()=>window.FMQGuidedHardwareTest.getState())).path).toBe('midi');\n});\n`;
write('browser-tests/quick-hardware-tests.spec.js',quickSpec);

// Durable docs: update the current acceptance path without claiming physical acceptance.
replaceOnce('HARDWARE_VALIDATION.md',
`## Guided in-app acceptance and report transfer (v2.6.8)\n\nStart from **Hardware & Backup → Run Hardware Test**. The guided flow records technical observations while the child follows plain-language prompts. It reuses the production Guitar audio service and shared Web MIDI service; it does not change scoring/detector thresholds or replace the manual checks below.`,
`## Guided in-app acceptance and report transfer (v2.6.14)\n\nStart from **Hardware & Backup → Quick Hardware Tests**. The child-facing flow records technical observations while the child follows plain-language prompts. It reuses the production Guitar audio service, the production monophonic Piano microphone detector, the shared Web MIDI service, and the existing Hardware Validation/report-transfer system; it does not change scoring/detector thresholds or replace the manual checks below.`,
'HARDWARE_VALIDATION guided heading');
replaceOnce('HARDWARE_VALIDATION.md',
`1. run Guitar audio if Guitar hardware is available;\n2. run Piano MIDI if MIDI hardware is available;`,
`1. run **Test Guitar Microphone** if Guitar hardware is available;\n2. run **Test Piano Microphone** for C4–D4–E4–F4–G4 plus repeated C4 when Piano microphone is being evaluated;\n3. run **Test MIDI Keyboard** if real MIDI hardware is available;`,
'HARDWARE_VALIDATION guided choices');
replaceOnce('HARDWARE_VALIDATION.md',
`3. answer the existing five Human Observations plus scoring-trust, adult-help and adult PASS/BLOCKER/NOT DECIDED fields;\n4. add optional child/tester notes or screenshot/video filenames when useful;\n5. open Report and prefer **Send Report to Parent**; choose the intended native share target such as Gmail and confirm the prepared report/attachments before sending;\n6. verify the recipient receives an understandable report plus structured evidence through the real Chromebook share path;\n7. if native sharing is unavailable or a target does not preserve the prepared evidence, use **Copy Project Report** and **Download JSON** as local fallbacks.`,
`4. answer the existing five Human Observations plus scoring-trust; adult/helper context remains distinct and the overall adult result stays NOT DECIDED unless an adult deliberately changes it;\n5. add optional child/tester notes or screenshot/video filenames when useful;\n6. use **Send Report to Parent**; choose the intended native share target such as Gmail and confirm the prepared report/attachments before sending;\n7. verify the recipient receives an understandable report plus structured evidence through the real Chromebook share path;\n8. if native sharing is unavailable or a target does not preserve the prepared evidence, use **Copy Project Report** and **Download JSON** as local fallbacks.`,
'HARDWARE_VALIDATION order');
replaceOnce('HARDWARE_VALIDATION.md',
`The guided quiet baseline is measurement-only. USB/MIDI disconnect/reconnect, Piano microphone, perceived latency, audio smoothness, child usability and actual ChromeOS share-sheet behavior remain manual Monday evidence.`,
`The guided quiet baselines are measurement-only. Completing the guided Piano microphone path truthfully records that short monophonic C4–G4/repeated-C4 hardware check; it does **not** certify Piano gameplay modes, chord recognition, calibrated latency or perceived responsiveness. USB/MIDI disconnect/reconnect, perceived latency, audio smoothness, child usability and actual ChromeOS share-sheet behavior remain physical evidence.`,
'HARDWARE_VALIDATION Piano truth');

replaceOnce('MONDAY_HARDWARE_TEST_PLAN.md',
`Before the longer gameplay matrix, open **Hardware & Backup → Run Hardware Test**.\n\n- Run **Guitar audio** with the intended microphone/USB input.\n- Run **Piano MIDI** when the physical MIDI keyboard is available.`,
`Before the longer gameplay matrix, open **Hardware & Backup → Quick Hardware Tests**. Keep each child-facing round short.\n\n- Tucker can run **Test Guitar Microphone** with the real guitar/amp and follow the quiet, six-string, repeated-note and silence prompts.\n- Nova can run **Test Piano Microphone** and follow the one-note-at-a-time C4–D4–E4–F4–G4 plus repeated-C4 prompts.\n- Run **Test MIDI Keyboard** only when a real MIDI keyboard is actually available; otherwise leave it not performed.`,
'MONDAY quick tests');
replaceOnce('MONDAY_HARDWARE_TEST_PLAN.md',
`- Open **Report** and verify **Copy Project Report**.\n- Paste the Project Report into the Family Music Quest Project Manager chat on the phone.\n- Use **Share Test Report** and verify the physical ChromeOS share sheet where supported. Use any available destination (Quick Share may or may not be offered by ChromeOS) to move the JSON to the Android phone.\n- Attach the JSON to the Family Music Quest Project for technical analysis.\n- Also verify **Download JSON** and the unsupported-share fallback.`,
`- After the short questions, use **Send Report to Parent** and verify the physical ChromeOS share sheet/Gmail path where supported.\n- Confirm the parent receives an understandable report/attachments.\n- Use **Copy Project Report** and **Download JSON** only as fallbacks when native transfer does not work.`,
'MONDAY transfer');

insertBefore('CHANGELOG.md',`## v2.6.13 — Child-Friendly Parent Report Transfer`,
`## v2.6.14 — Child Self-Guided Hardware Tests\n\n- Renamed the child-facing guided entry to **Quick Hardware Tests** with obvious Guitar microphone, Piano microphone and MIDI keyboard choices.\n- Reused the existing guided Guitar and MIDI paths without changing detector/scoring behavior.\n- Added a guided monophonic Piano microphone check using the production Piano microphone detector: quiet baseline, C4–D4–E4–F4–G4 and repeated C4 with plain retry feedback.\n- Added structured guided Piano microphone evidence to the existing Hardware Validation session/report, so a completed guided check is no longer reported as unperformed while gameplay/perceived-response acceptance remains explicitly manual.\n- Made the existing five child questions follow successful real quick tests and made the existing v2.6.13 **Send Report to Parent** flow the obvious completion action.\n- No scoring windows, microphone thresholds, save schema, #24/#34 behavior, USB Guitar behavior, Bass work, or full Hardware Setup Wizard were added.\n- Physical Dell Chromebook microphone/child-usability/wake-lock/Gmail acceptance remains required.\n\n`,
'v2.6.14 changelog entry');

replaceOnce('ROADMAP.md',
`Issue #30 shipped technically in v2.6.12 and remains subject to real Dell Chromebook wake-lock acceptance. Issue #31 is the focused v2.6.13 child-friendly parent-report-transfer maintenance release and likewise requires physical ChromeOS/Gmail verification after deployment. Its separate manual-evidence-fidelity concern is tracked in Backlog Issue #34. Issue #24 remains a separate, non-blocking Backlog reporting issue about distinguishing an enumerated virtual/system MIDI endpoint from a verified playable keyboard. Neither #24/#34 nor Bass work is included in this maintenance sequence.`,
`Issue #30 shipped technically in v2.6.12 and remains subject to real Dell Chromebook wake-lock acceptance. Issue #31 shipped technically in v2.6.13 and likewise requires physical ChromeOS/Gmail verification. Issue #36 is the focused v2.6.14 child self-guided hardware-test maintenance release: it extends the existing Guided Hardware Test with an honest production-detector Piano microphone path and child-facing Quick Hardware Tests so the remaining evidence can be collected in short independent rounds. Its physical microphone/child-usability acceptance remains part of the current gate. The separate manual-evidence-fidelity concern remains Backlog Issue #34. Issue #24 remains a separate, non-blocking Backlog reporting issue about distinguishing an enumerated virtual/system MIDI endpoint from a verified playable keyboard. Neither #24/#34 nor Bass work is included in this maintenance sequence.`,
'ROADMAP current maintenance state');

replaceOnce('TECHNICAL_DEBT.md',
`v2.6.7/v2.6.8 now provide a child-friendly acceptance test, structured report, human evidence and local report transfer while preserving Advanced Diagnostics. The broader first-use hardware setup wizard remains later scope: connection-method selection, persistent readiness/setup reuse, richer recovery and optional future latency calibration are not implemented yet.`,
`v2.6.7/v2.6.8 established the child-friendly acceptance/report foundation, v2.6.13 simplified parent transfer, and v2.6.14 adds short self-guided Guitar microphone, monophonic Piano microphone and MIDI choices over the same production services/report session. The broader first-use Hardware Setup Wizard remains later scope: connection-method selection, persistent readiness/setup reuse, richer recovery and optional future latency calibration are not implemented by these quick acceptance tests.`,
'TECHNICAL_DEBT guided setup status');

replaceOnce('ARCHITECTURE.md',
`Current capabilities include:\n\n- production Guitar microphone observation;`,
`Current capabilities include:\n\n- child-facing **Quick Hardware Tests** layered over the existing guided acceptance/report system for Guitar microphone, monophonic Piano microphone, and shared Web MIDI;\n- production Guitar microphone observation;`,
'ARCHITECTURE quick tests');
replaceOnce('ARCHITECTURE.md',
`- Web MIDI connection and live event diagnostics;`,
`- guided Piano microphone evidence that instantiates the production Piano microphone detector and consumes its already-gated stable/quiet readings rather than maintaining a second detector;\n- Web MIDI connection and live event diagnostics;`,
'ARCHITECTURE Piano quick test reuse');

// Remove temporary implementation machinery from the final branch tree.
for(const path of ['tools/tmp-v2614-apply.mjs','.github/workflows/tmp-v2614-apply.yml']){
  try{fs.rmSync(path);}catch{}
}
console.log('Applied focused v2.6.14 child self-guided hardware-test release changes.');
